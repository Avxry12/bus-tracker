function trackBus() {
    alert("Tracking feature coming soon!");
}

// Placeholder code to initialize a map (replace with your map API implementation).
document.addEventListener("DOMContentLoaded", function() {
    if (document.getElementById("map")) {
        document.getElementById("map").innerHTML = "Map will be here!";
    }
    if (document.getElementById("live-map")) {
        document.getElementById("live-map").innerHTML = "Live map coming soon!";
    }
});
